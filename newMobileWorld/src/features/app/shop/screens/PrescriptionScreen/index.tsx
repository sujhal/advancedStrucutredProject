import React from 'react';

import CenteredMessageScreen from '@components/layout/CenteredMessageScreen';

const PrescriptionScreen = (): React.JSX.Element => {
  return (
    <CenteredMessageScreen title="Prescription" subtitle="Prescription upload and tracking." />
  );
};

export default PrescriptionScreen;
