import React from 'react';

import CenteredMessageScreen from '@components/layout/CenteredMessageScreen';

const SellersScreen = (): React.JSX.Element => {
  return <CenteredMessageScreen title="Sellers" subtitle="Sellers directory." />;
};

export default SellersScreen;
